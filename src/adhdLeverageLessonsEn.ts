/**
 * ADHD Leverage Course — English placeholder
 *
 * The English translation is not yet authored. Until it lands, fall back to
 * the Japanese map so the course is at least navigable in EN builds (same
 * approach as the career-* courses).
 *
 * When proper English content is authored, replace this re-export with a
 * locally-defined map of `LessonData` entries (IDs 800–807).
 */
export { adhdLeverageLessonMap as adhdLeverageLessonMapEn } from './adhdLeverageLessons'
