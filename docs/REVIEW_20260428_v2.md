# Reviewer 判定: REJECTED

- レビュー日時: 2026-04-28T09:00:00+09:00
- コミット: eea094b
- レビュアー: Reviewer (自動品質ゲート)

## 判定: REJECTED

## 確認結果

### 1. スクショ実在

**✅ PASS**

```
-rw-rw-r-- 1 work work 280182 Apr 28 08:34 docs/screenshots/qa-20260428-pricing.png
```

ファイルサイズ: **280,182 bytes (約 274 KB)** — 1KB 以上の条件を満たす。

---

### 2. Google Play 実検証

**✅ PASS**

`server/index.ts` L1556〜1601 に、`GOOGLE_PLAY_PRIVATE_KEY` / `GOOGLE_PLAY_PACKAGE_NAME`
の env var 条件分岐つきで Google Play Developer API (androidpublisher v3) の実検証ロジックが実装されている。

- `GoogleAuth` / `google.androidpublisher` による `purchases.subscriptions.get` 呼び出し確認済み
- `paymentState` と `expiryTimeMillis` で有効性検証 → 無効時は 400 を返す実装確認済み
- env var 未設定時はスキップ（フォールバック）ログを出力する条件分岐確認済み

---

### 3. catch e: unknown

**🔴 FAIL**

`/api/billing/verify` エンドポイント自体の catch ブロック（L1639）は `e: unknown` に修正されている：

```typescript
// L1639 ✅
} catch (e: unknown) {
```

しかし、`server/index.ts` 全体には **`catch (e: any)` が大量に残存** している：

```
L118:   catch (err: any)
L200:   catch (e: any)
L788:   catch (e: any)
L954:   catch (e: any)
L1121:  catch (e: any)
L1169:  catch (e: any)
L1192:  catch (e: any)
L1232:  catch (e: any)
L1257:  catch (e: any)
L1301:  catch (e: any)
L1363:  catch (e: any)
L1434:  catch (emailErr: any)
L1452:  catch (e: any)
L1475:  catch (e: any)
L1530:  catch (e: any)
L1551:  catch (e: any)
L1672:  catch (e: any)
L1708:  catch (e: any)
L1816:  catch (e: any)
L1878:  catch (e: any)
L1918:  catch (e: any)
L1951:  catch (e: any)
```

前回 REJECT 指摘「`catch (e: any)` mixed in — must be fixed to `e: unknown`」に対して、
`/api/billing/verify` の catch ブロックのみ修正されたが、**ファイル全体に `e: any` が混在している状況は解消されていない**。

> ⚠️ 判定基準「`catch (e: any)` mixed in — must be fixed to `e: unknown`」は
> ファイル全体に `e: any` が **混在している状態そのもの**を指している。
> 当該エンドポイントのみの部分修正では条件を満たさない。

---

### 4. エラーパスエビデンス

**✅ PASS**

QA レポート `QA_REPORT_20260428_v2.md` にて以下のエラーパスが確認済み：

| テスト | リクエスト | レスポンス | HTTP | 判定 |
|-------|-----------|----------|------|------|
| 不明 productId | `{"purchaseToken":"test-token-123","productId":"invalid_product_xyz"}` | `{"error":"Unknown productId: invalid_product_xyz"}` | **400** | ✅ |
| purchaseToken 空 | `{"purchaseToken":"","productId":"logic_premium_monthly"}` | `{"error":"purchaseToken and productId are required"}` | **400** | ✅ |

---

## 結論

**REJECTED — 差し戻し**

1件の指摘が未解消のため、本番マージ不可。

### 🔴 差し戻し内容

**`server/index.ts` 内の `catch (e: any)` / `catch (err: any)` をすべて `catch (e: unknown)` に統一すること。**

現在 22 箇所の `any` 型 catch が残存している（L118, L200, L788, L954, L1121, L1169, L1192, L1232, L1257, L1301, L1363, L1434, L1452, L1475, L1530, L1551, L1672, L1708, L1816, L1878, L1918, L1951）。

各 catch ブロック内で `e` を参照している箇所は、`e instanceof Error ? e.message : String(e)` 等の型ガードに置き換えること。修正後、`npx tsc --noEmit` がエラーなしで通ることを確認してから再提出すること。

### ✅ 解消済み指摘（前回 REJECT より）

- スクショ実在: qa-20260428-pricing.png (274KB) 確認済み ✅
- Google Play 実検証ロジック: env var 条件分岐つきで実装済み ✅
- エラーパスエビデンス: 400 レスポンス 2パターン確認済み ✅
