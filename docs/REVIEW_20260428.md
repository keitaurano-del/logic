# Reviewer 判定: REJECTED

- レビュー日時: 2026-04-28 07:19 JST
- コミット: a73e532
- 変更内容: SCRUM-116 Google Play Billing 抽象レイヤー導入
- QA レポート: docs/QA_REPORT_20260428.md
- レビュアー: Reviewer (自動品質ゲート)

---

## 不合格理由

### 1. ❌ PricingScreen スクショが存在しない【即 REJECT 条件】

QA レポート（#8 料金プラン画面）には以下の記載がある:

> スクショ: docs/screenshots/qa-20260428-pricing.png ✅（実在確認: **目視確認済み**）

しかし実際にファイルシステムを確認したところ、**当該ファイルは存在しない**:

```
ls: cannot access '.../qa-20260428-pricing.png': No such file or directory
```

スクショ一覧テーブルにも `qa-20260428-pricing.png` はサイズ付きで記載されておらず、
「目視確認済み」は証跡として認められない。

**PricingScreen（`src/screens/PricingScreen.tsx`）は SCRUM-116 の変更対象ファイルであり、
エラーメッセージ改善が含まれている。変更画面のスクショ欠如は即 REJECT。**

---

### 2. ⚠️ /api/billing/verify — Google Play 実検証が未実装（リスク明記なし）

`server/index.ts` L1570–1578 の確認:

```typescript
// TODO(SCRUM-116): Verify purchase with Google Play Developer API
// Requires GOOGLE_PLAY_PRIVATE_KEY and GOOGLE_PLAY_PACKAGE_NAME env vars
// const auth = new GoogleAuth(...)
// const androidpublisher = google.androidpublisher(...)
```

現状エンドポイントは **purchaseToken を Google Play Developer API で検証せず**、
`productId` の文字列マッチングだけでサブスクリプションを有効化する。
QA レポートにこのリスクへの言及・承認記録がない。

- テストで使用された `purchaseToken: "test-token"` は偽トークンである
- 本番で任意の `purchaseToken` を送信すれば無制限にサブスクリプション付与が可能
- リリースノートまたは QA レポートに「Google Play 実検証は将来対応 / 本番前にブロックする」旨の明示が必要

---

### 3. ⚠️ catch ブロックに `e: any` が混入（新規エンドポイント）

`server/index.ts` L1618:

```typescript
} catch (e: any) {
```

SCRUM-116 で追加された `/api/billing/verify` エンドポイント内の catch ブロックで `any` が使用されている。
`src/billing/index.ts` 側は `any` なしで実装されており品質基準を満たしているが、
server 側の新規コードは基準を満たしていない。

---

### 4. ⚠️ /api/billing/verify — エラーパスのテスト不足

QA では正常系（`logic_premium_monthly`）のみ確認。以下が未検証:

| テストケース | 状態 |
|---|---|
| 不明な productId → 400 エラー | ❌ 未確認 |
| purchaseToken 空文字 → 400 エラー | ❌ 未確認 |
| productId 空文字 → 400 エラー | ❌ 未確認 |

コードは正しく実装されているが、QA エビデンスがない。

---

## スクショ実在確認結果

| ファイル | 実サイズ | 判定 |
|---------|--------|------|
| qa-20260428-home.png | 990,824 bytes | ✅ |
| qa-20260428-roadmap.png | 1,032,146 bytes | ✅ |
| qa-20260428-stats.png | 951,617 bytes | ✅ |
| qa-20260428-profile.png | 1,025,076 bytes | ✅ |
| **qa-20260428-pricing.png** | **ファイルなし** | **❌ REJECT** |

---

## コード品質確認結果

### src/billing/index.ts
| 確認項目 | 結果 |
|---------|------|
| `any` 使用なし | ✅ (`window as unknown as Record<string, unknown>` で適切に回避) |
| 秘密鍵・APIキーのハードコードなし | ✅ |
| プラグイン利用不可時のフォールバック | ✅ (stub モードで適切に動作) |

### server/index.ts — /api/billing/verify
| 確認項目 | 結果 |
|---------|------|
| purchaseToken 必須チェック | ✅ L1566–1568 |
| productId 必須チェック | ✅ L1566–1568 |
| 不明な productId → 400 | ✅ L1591–1593 |
| catch ブロックの `e: any` | ⚠️ L1618 |
| Google Play Developer API 実検証 | ❌ 未実装（TODO コメントのみ） |

---

## Keita-san 過去指摘との照合

| 指摘 | QA確認 | 証跡 |
|------|--------|------|
| 背景 #082121系ダーク | ✅ | home/roadmap/stats/profile スクショ実在 |
| カードが背景に埋もれていないか | ✅ | スクショ実在 |
| カテゴリタップで空一覧にならないか | ✅ | ロジカルシンキング9件確認 |
| 記録・プロフィールが動いているか | ✅ | stats/profile スクショ実在 |

---

## QA への差し戻し内容

### 再テスト必須範囲

1. **料金プラン画面（PricingScreen）**
   - スクショ撮影・保存: `docs/screenshots/qa-20260428-pricing.png`
   - 確認観点:
     - エラーメッセージ改善（SCRUM-116 変更箇所）が正しく表示されるか
     - ベーシック/スタンダード/プレミアム 3プラン表示
     - 月払い/年払い切替
     - Web 環境での Stripe 動線

2. **/api/billing/verify エラーパステスト**（エビデンス添付必須）
   - 不明な productId（例: `"invalid_product"`）→ 400 + エラー JSON 確認
   - purchaseToken 空文字 → 400 確認

### 不足エビデンス

| エビデンス | 対応 |
|-----------|------|
| `qa-20260428-pricing.png`（実ファイル） | 再撮影・保存 |
| 不明 productId → 400 のレスポンス | curl/Postman 結果を貼付 |
| purchaseToken 空 → 400 のレスポンス | curl/Postman 結果を貼付 |

### CTO への確認依頼（ブロッカー候補）

> **Google Play 実検証の未実装について**
> 現在 `/api/billing/verify` は purchaseToken を Google Play Developer API で検証しておらず、
> 任意のトークンでサブスクリプションが付与される状態です。
> 本番リリース前にこの動作を許容するか、または SCRUM-116 のスコープとして
> 実装完了を必須とするか、方針を確認してください。
> - 該当ファイル: `server/index.ts` L1570–1578
> - 関連 TODO: `TODO(SCRUM-116)`

---

## 合格条件（再提出時チェックリスト）

- [ ] `qa-20260428-pricing.png` が 1KB 以上のサイズで実在する
- [ ] PricingScreen のエラーメッセージ改善が目視確認されている
- [ ] 不明 productId → 400 のエビデンスが添付されている
- [ ] Google Play 実検証未実装について CTO 承認または方針決定が記録されている
