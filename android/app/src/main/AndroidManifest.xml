<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="logic" android:host="auth" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Local Notifications (Capacitor LocalNotifications plugin)
         - POST_NOTIFICATIONS: Android 13+ で通知表示に必須 (ランタイム要求)
         - SCHEDULE_EXACT_ALARM: Android 12+ で setExactAndAllowWhileIdle を使うために宣言。
           ユーザーは設定画面で許可することで Doze / App Standby 下でも遅延なく発火する。
           未許可時は inexact フォールバックで遅延発火される (アプリは動作継続)。
         - RECEIVE_BOOT_COMPLETED: 端末再起動後にスケジュール済みリマインダーを復元する
           (LocalNotifications プラグインの BootReceiver が利用) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- TTS バックグラウンド再生 keep-alive (src/ttsService.ts)
         - WAKE_LOCK: navigator.wakeLock 経由で画面オフ抑止を試みる際に Android 側で必要。
           HTMLAudioElement の silent loop とセットで Tier 1 BG 再生を成立させる。
         - FOREGROUND_SERVICE / FOREGROUND_SERVICE_MEDIA_PLAYBACK: Tier 3 で MediaSessionService
           化する際に必須となる宣言。現時点では未使用だが、将来の Foreground Service 化に備えて
           manifest 側だけ先に通しておく (permission 宣言だけでは権限は付与されない)。
         費用は一切発生しない (OS 内蔵 TextToSpeech 経由)。 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
</manifest>
