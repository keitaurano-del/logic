<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
      Android 11 以下用の Launch テーマ。
      Capacitor の SplashScreen plugin は core-splashscreen compat ライブラリ経由で
      installSplashScreen() を呼ぶため、parent は Theme.SplashScreen に統一する。
      windowSplashScreenAnimatedIcon に透明 drawable、background に #1A1F2E を指定して
      「ロゴ無しの #1A1F2E 一色」が一瞬だけ出る挙動にする。

      Android 12+ では values-v31/styles.xml の同名テーマが優先される。
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_transparent</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
