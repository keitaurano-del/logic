git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")
type("appstore") # default; lanes override with `app_store` / `development`

app_identifier([ENV["APP_BUNDLE_ID"] || "io.logic.app"])
username(ENV["APPLE_ID"])
team_id(ENV["DEVELOPER_TEAM_ID"])
